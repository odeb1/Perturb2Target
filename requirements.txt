# Core analysis (steps 2-8)
numpy>=1.24
pandas>=2.0
scipy>=1.10
matplotlib>=3.7
pyarrow>=12.0          # parquet I/O

# Step 1 — stream the source .h5ad from S3
anndata>=0.9
h5py>=3.8
s3fs>=2023.6
boto3>=1.28

# Step 4a — Open Targets GraphQL (portable HTTP fallback; in-platform uses the clinical-genomics MCP)
requests>=2.31

# Report rendering (optional; findings_report.md -> .pdf)
markdown>=3.4
xhtml2pdf>=0.2.11

# Notebook
nbformat>=5.9

# Figures use apply_figure_style()/panel_letter() from the Claude Science `figure-style` skill.
# Outside that environment, the figure scripts run with plain matplotlib defaults if those
# two helpers are stubbed to no-ops.
